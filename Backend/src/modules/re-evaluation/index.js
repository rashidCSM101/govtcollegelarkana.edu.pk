const service = require('./re-evaluation.service');
const controller = require('./re-evaluation.controller');
const routes = require('./re-evaluation.routes');

module.exports = {
  service,
  controller,
  routes
};
